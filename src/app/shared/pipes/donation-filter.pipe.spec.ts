import { DonationFilterPipe } from './donation-filter.pipe';

describe('DonationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DonationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
